{
  "name": "optionscan-pcr",
  "version": "1.0.0",
  "description": "Marketdata.app PCR tracker API and frontend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "pages:dev": "wrangler pages dev public",
    "pages:deploy": "wrangler pages deploy public --project-name=optionscan"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  },
  "engines": {
    "node": ">=18"
  }
}
